import React from "react";
import "./Toolbar.css"
import Logo from "../../Logo/Logo";
const toolbar = (props) => (
    <header className="Toolbar" >
        <div>menu</div>
        <Logo/>
        <nav>...</nav>
    </header>
)

export default toolbar;