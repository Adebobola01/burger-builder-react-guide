import React from "react";
import "./Toolbar.css"

const toolbar = (props) => (
    <header className="Toolbar" >
        <div>menu</div>
        <div>logo</div>
        <nav>...</nav>
    </header>
)

export default toolbar;